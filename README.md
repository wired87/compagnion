# brainmaster
A modular Brain that acts as a user - sys interface. it consumes given MCPs, plans, executes and interacts witht the user - fully local (no cloud based LLM include - but can be extend) 

Pls post issue requests if you like it
